// const templateString = `this is a
// a template string.

// updated!!
// `
// console.log(templateString)

const name = 'shamim';
const number = 1122;
const templateString = `this is a
a template string.${name.length}
----------------------------------
${number}
updated!!
<h1>Hello</h1>
`
console.log(templateString)